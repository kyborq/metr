import { createBrowserRouter } from "react-router";

const router = createBrowserRouter([
  {
    path: "/",
    element: null,
  },
  {
    path: "/demo",
    element: null,
  },
]);

export default router;
